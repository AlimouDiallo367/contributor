<UserControl x:Class="TP1_Donateurs.Views.ContributionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="900">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
       
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> 
                <ColumnDefinition Width="150"/>   <!-- Space -->
                <ColumnDefinition Width="Auto"/> 
                <ColumnDefinition Width="150"/> <!-- Space -->
                <ColumnDefinition Width="Auto"/> 
            </Grid.ColumnDefinitions>
           
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Ajouter" Command="{Binding CmdImporterFichier}" Width="100" Margin="0,0,10,0"/>
                <Button Content="Effacer" Width="100"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                <TextBlock Text="Décompte" VerticalAlignment="Center"/>
                <TextBlock Text="0" Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Illégales seulement" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <CheckBox IsChecked="False" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1"  Margin="10" ItemsSource="{Binding LesContributions}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="80"/>
                    <GridViewColumn Header="Prénom"  DisplayMemberBinding="{Binding Prenom}" Width="80"/>
                    <GridViewColumn Header="Nom" DisplayMemberBinding="{Binding Nom}" Width="80"/>
                    <GridViewColumn Header="Montant" DisplayMemberBinding="{Binding Montant}" Width="80"/>
                    <GridViewColumn Header="Nb. Versements" DisplayMemberBinding="{Binding NbVersements}" Width="100"/>
                    <GridViewColumn Header="Municipalité" DisplayMemberBinding="{Binding Municipalite}" Width="100"/>
                    <GridViewColumn Header="Code postal"  DisplayMemberBinding="{Binding CodePostal}" Width="80"/>
                    <GridViewColumn Header="Parti" DisplayMemberBinding="{Binding Parti}" Width="80"/>
                    <GridViewColumn Header="Candidat" DisplayMemberBinding="{Binding Candidat}" Width="80"/>
                    <GridViewColumn Header="Date évènement" DisplayMemberBinding="{Binding DateEvenement}" Width="100"/>
                    <GridViewColumn Header="Année financière" DisplayMemberBinding="{Binding AnneeFinanciere}" Width="100"/>
                    <GridViewColumn Header="Illégale?"  DisplayMemberBinding="{Binding Illegale}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
