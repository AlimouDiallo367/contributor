<UserControl x:Class="TP1_Donateurs.Views.ContributionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:p="clr-namespace:TP1_Donateurs.Properties"
             xmlns:converters="clr-namespace:TP1_Donateurs.ViewModels.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="900">
    <UserControl.Resources>
        <converters:BoolToIllegaleConverter x:Key="boolToIllegale"/>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
       
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> 
                <ColumnDefinition Width="150"/>   <!-- Space -->
                <ColumnDefinition Width="Auto"/> 
                <ColumnDefinition Width="150"/> <!-- Space -->
                <ColumnDefinition Width="Auto"/> 
            </Grid.ColumnDefinitions>
           
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="{x:Static p:traduction.btn_ajouter}" Command="{Binding CmdImporterFichier}" Width="100" Margin="0,0,10,0"/>
                <Button Content="{x:Static p:traduction.btn_effacer}" Command="{Binding CmdEffacer}" Width="100"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                <Label Content="{x:Static p:traduction.lbl_decompte}" VerticalAlignment="Center"/>
                <Label Content="{Binding Path=LesContributions.Count}" Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                <Label Content="{x:Static p:traduction.lbl_illegales_seulement}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <CheckBox IsEnabled="{Binding IsEnabled}" IsChecked="{Binding AfficherContributionsIllegales, Mode=TwoWay}" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1"  Margin="10" ItemsSource="{Binding LesContributions}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_type}" DisplayMemberBinding="{Binding Type}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_prenom}"  DisplayMemberBinding="{Binding Prenom}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_nom}" DisplayMemberBinding="{Binding Nom}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_montant}" DisplayMemberBinding="{Binding Montant}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_nb_versements}" DisplayMemberBinding="{Binding NbVersements}" Width="100"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_municipalite}" DisplayMemberBinding="{Binding Municipalite}" Width="100"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_code_postal}"  DisplayMemberBinding="{Binding CodePostal}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_parti}" DisplayMemberBinding="{Binding Parti}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_candidat}" DisplayMemberBinding="{Binding Candidat}" Width="80"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_evenement}" DisplayMemberBinding="{Binding DateEvenement}" Width="100"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_annee_financiere}" DisplayMemberBinding="{Binding AnneeFinanciere}" Width="100"/>
                    <GridViewColumn Header="{x:Static p:traduction.lbl_illegale}"  DisplayMemberBinding="{Binding EstIllegale, Converter={StaticResource boolToIllegale}}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
