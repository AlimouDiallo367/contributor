<Window x:Class="TP1_Donateurs.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TP1_Donateurs.Views"
        xmlns:viewmodels="clr-namespace:TP1_Donateurs.ViewModels"
        mc:Ignorable="d"
        Title="Donateur" Height="620" Width="960" x:Name="win" Icon="/Resources/election.ico">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:ContributionViewModel}">
            <local:ContributionView/>
        </DataTemplate> 
        <DataTemplate DataType="{x:Type viewmodels:ConfigurationViewModel}">
            <local:ConfigurationView/>
        </DataTemplate>
    </Window.Resources> 
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Configuration" Command="{Binding CmdGotoConfiguration}"/>
        </Menu>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"></ContentControl>
    </Grid>
</Window>
